<li>
    <h4>
    {{# if isbn}}
        <a href="http://www.amazon.com/gp/search?index=books&amp;field-isbn={{isbn_snip}}">{{title}}</a>
    {{else}}
        {{title}}
    {{/if}}
    </h4>
    {{#if author}}
        <p>by {{author}}</p>
    {{/if}}
</li>


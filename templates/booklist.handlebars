<div>
    <h1>Booklist</h1>
    {{#each neighborhoods}}
        <div>
            <h2>{{name}}</h2>
            {{#each organizations}}
                <div>
                    <h3>{{organization}}</h3>
                    <ul>
                        {{#each books}}
                            {{>book }}
                        {{/each}}
                    </ul>
                </div>
            {{/each}}
        </div>
    {{/each}}
</div>
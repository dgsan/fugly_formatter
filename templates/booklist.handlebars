<div class="science-custom-style">
    {{#each neighborhoods}}
        <div>
            <h2 class="{{color_class}}">{{name}}</h2>
            {{#each organizations}}
                <div>
                    <h3>{{organization}}</h3>
                    <ul>
                        {{#each books}}
                            {{>book }}
                        {{/each}}
                    </ul>
                </div>
            {{/each}}
        </div>
    {{/each}}
</div>